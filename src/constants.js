import { bot_name, token, channel, ban } from '/home/subo/subo_bot_constants';
export const channel_name = channel
export const oauth_token = token
export const bot_username = bot_name
export const ban_words = ban